{
  "name": "@aether/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for building on the Aether blockchain",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node --eval \"import('node:fs').then(fs => fs.rmSync('dist', { recursive: true, force: true }))\"",
    "test": "npm run build && node --test dist/tests/*.js"
  },
  "keywords": [
    "aether",
    "blockchain",
    "sdk"
  ],
  "author": "Aether Labs",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "types": "src/index.ts",
  "dependencies": {
    "@noble/ed25519": "^1.7.5",
    "@noble/hashes": "^1.8.0"
  }
}
